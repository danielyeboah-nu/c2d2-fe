<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!-- ATAK plugin descriptor — required by ATAK plugin loader -->
        <meta-data
            android:name="com.atakmap.app.COMPONENTS"
            android:value="com.c2d2.atak.C2D2PluginLifecycle" />

        <!-- Background sync service -->
        <service
            android:name=".service.BackendSyncService"
            android:enabled="true"
            android:exported="false" />

        <!-- Intent filter so ATAK can discover the plugin -->
        <receiver
            android:name=".C2D2DropDownReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.c2d2.atak.SHOW_PANEL" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
